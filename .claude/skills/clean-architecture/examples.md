# Examples

- Use when: Usa quando decidi in che layer va una funzionalità, come disaccoppiare backend dal DB o dal framework, o per revisionare i confini tra moduli del Control Plane (config_reader, activation_engine, ecc. da ARCHITECTURE.md).
- Do not use when another skill is more specific to the stack, tool, or risk profile.
