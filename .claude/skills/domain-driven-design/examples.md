# Examples

- Use when: Usa per modellare i concetti di dominio del Control Plane (package, activation, telemetry, session) con terminologia coerente tra backend e UI.
- Do not use when another skill is more specific to the stack, tool, or risk profile.
